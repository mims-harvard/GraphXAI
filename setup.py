from setuptools import setup

setup(name='graphxai',
      version='0.1',
      description='algorithms and metrics for explaining graph neural networks',
      packages=['graphxai'],
      zip_safe=False)
